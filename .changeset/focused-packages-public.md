---
"counterfact": patch
---

Publish additive focused package APIs for OpenAPI loading, generation, runtime
embedding, reusable clients, REPL integration, and shared TypeScript contracts
while preserving the existing `counterfact` facade.
