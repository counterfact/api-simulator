overlay: 1.0.0
info:
  title: Update API info
  version: 1.0.0
actions:
  # Overwrite the API title and description in the info object.
  - target: $.info
    update:
      title: Sample API (Staging)
      description: Staging environment — not for production use.
      contact:
        name: Platform Team
        email: platform@example.com
