openapi: 3.0.3
info:
  title: Runtime MSW fixture
  version: 1.0.0
paths:
  /foo:
    get:
      responses:
        "200":
          description: A successful response
          content:
            application/json:
              schema:
                type: object
