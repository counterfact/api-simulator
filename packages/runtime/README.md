# `@counterfact/runtime`

Counterfact's HTTP runtime, route and context registries, module loading,
validation, proxying, hot reload, Koa integration, and MSW adapter.

The package is private while its direct-consumer API is stabilized. Counterfact
uses only the exports declared by this package.
